apiVersion: v1
kind: Service
metadata:
  name: NAME
  namespace: NAMESPACE
spec:
  selector:
    app.kubernetes.io/name: NAME
  ports:
    - protocol: TCP
      port: PORT
      targerPort: TARGETPORT
  type: LoadBalancer
